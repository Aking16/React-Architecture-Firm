import { arrowUp } from "../assets";

const button = () => (
  <div className="flex bg-primary p-5 rounded-full border border-secondary">
    <img src={arrowUp} />
  </div>
);

export default button;
